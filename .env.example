VITE_API_MODE=backend
VITE_API_BASE_URL=/api/v1
# API 联调测试框：开发环境默认显示、生产环境默认隐藏；可显式设为 true 或 false 覆盖。
# VITE_SHOW_API_TEST_PANEL=true
# 真实后端模式下由 Vite 同源代理转发，避免浏览器跨域限制。
# VITE_API_PROXY_TARGET=http://127.0.0.1:8123
